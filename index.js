import { mountApp, mount, patch, h } from "./core/render";
import { watchEffect, reactive } from "./core/reactivity/reactive";

export {
  mountApp,
  mount,
  patch,
  h,
  watchEffect,
  reactive,
}
